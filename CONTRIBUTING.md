# المساهمة في ArCode-Core
## Contributing

نرحب بالمساهمات لبناء معيار عربي موحد للمصطلحات العلمية. اقرأ أولاً:

- `naming_rules.md` — قواعد التسمية
- `terminology_process.md` — منهجية اعتماد المصطلحات
- `id_system.md` — نظام المعرفات

---

## كيف تقترح مصطلحاً جديداً؟

1. افتح Issue وفق قالب "اقتراح مصطلح" (`اقتراح مصطلح جديد`).
2. أرسل Pull Request يضيف المصطلح إلى `dictionary.json` مع:
   - `ACS_ID` برقم تسلسلي جديد غير مستخدم (راجع `id_system.md`).
   - `DOMAIN` مطابق تماماً لأسماء `domain_list.json`.
   - `STATUS: "Draft"` (لا تُعتمد مصطلحات في PR مباشرة).
   - `SOURCE` من المصادر المعتمدة في `terminology_process.md`.
3. شغّل الفحص:

   ```bash
   npm run validate
   ```

   يجب أن تمر جميع الفحوصات قبل قبول الـ PR.

---

## ملاحظات عامة

- لا تُعدّل أو تحذف أي `ACS_ID` معتمداً. أي تصحيح للتعريف = إصدار جديد (`VERSION`).
- الالتزام بالعربية الفصحى في كل المحتوى العربي.
- كل تعريف قصير وواضح، بلا فلسفة زائدة.
- الـ PRs تكون صغيرة وقابلة للمراجعة (مصطلح واحد أو مجموعة صغيرة من نفس المجال).
