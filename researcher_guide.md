# دليل الباحث: منهجية إضافة المصطلحات

هذا الدليل يشرح كيف تُضاف المصطلحات الجديدة إلى قاموس ArCode-Core، وما
الضوابط المطلوبة لضمان اتساق المعيار وجودته.

## 1. مسار المصطلح (دورة الحياة)

1. **اقتراح** — يُفتح Issue من قالب `term_proposal` في GitHub.
2. **بحث وتوثيق** — يتحقق الباحث من وجود المصطلح في المصادر الموثوقة.
3. **مراجعة** — يراجع محرر/باحث آخر المصطلح والترجمة والتعريف.
4. **إضافة** — تُضاف البطاقة إلى `dictionary.json` بحالة `Draft`.
5. **اعتماد** — بعد اجتياز المراجعة تُحوَّل الحالة إلى `Approved`.
6. **إهمال** — عند وجود تعارض تُوضع الحالة `Deprecated` مع إبقاء السجل.

## 2. المتطلبات الإلزامية لكل مصطلح

| الحقل | الشرط |
|-------|-------|
| `ACS_ID` | بصيغة `ACS-XXX-0000`، فريد، لا يُعاد استخدامه أبداً |
| `AR_TERM` | عربية فصحى، لا مختصرات لاتينية، لا أقواس رومانية |
| `EN_TERM` | إنجليزية سليمة |
| `DOMAIN` | مجال واحد فقط من `domain_list.json` |
| `DEFINITION_AR` | تعريف واضح لا يقل عن ~20 حرفاً، ينتهي بنقطة |
| `DEFINITION_EN` | تعريف إنجليزي مكافئ لا يقل عن ~20 حرفاً |
| `STATUS` | `Draft`، `Approved`، أو `Deprecated` |
| `SOURCE` | مرجع موثوق (ISO، RFC، مجمع اللغة العربية، منظمات متخصصة…) |

## 3. قواعد التسمية العربية

- اختر المصطلح العربي المتداول في المراجع الموثوقة.
- تجنب الاقتراض الصوتي (كالترجمة الحرفية للمختصرات) ما أمكن.
- إذا استُخدم مصطلح أجنبي شائع (مثل اسم علم أو مختصر عالمي)، أبقِه داخل
  التعريف لا في حقل المصطلح.
- لا تُكرر المصطلح العربي أو الإنجليزي داخل المجال الواحد (يفحصها
  `validate.js` تلقائياً).

## 4. المصادر المعتمدة

- المعايير الدولية: ISO/IEC 2382، ISO 80000-series، RFC، IEEE، NIST.
- المعاجم الموحدة: المعجم الطبي الموحد (منظمة الصحة العالمية)، المصطلحات
  العلمية الموحدة (الألكسو)، معاجم مجمع اللغة العربية.
- منظمات متخصصة: الفاو، الاتحاد الفلكي الدولي، المسح الجيولوجي الأمريكي،
  اللجنة الإحصائية الدولية.

## 5. التحقق الآلي

قبل رفع أي تغيير شغّل:

```bash
npm run validate
```

يجب أن يُطبع `ArCode validation passed: 18000 terms, all consistent.`
(أو العدد الحالي). GitHub Actions يشغّل نفس التحقق عند كل Pull Request.

## 6. خطوات عملية للإضافة

1. انسخ بنية البطاقات المجاورة في `dictionary.json`.
2. خصّص الرقم التسلسلي التالي في مجال المصطلح.
3. أدخل الحقول الثمانية كاملة.
4. شغّل `node validate.js` وتأكد من النجاح.
5. افتح Pull Request؛ سيتحقق CI تلقائياً.
6. اطلب مراجعة من مساهم آخر قبل الاعتماد.
